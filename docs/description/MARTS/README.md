# Business Descriptions — RETAIL_DWH.MARTS

This folder contains business descriptions for objects in schema `RETAIL_DWH.MARTS`.

> Note: No tables were provided in the current run (`tables=[]`). This README is initialized to satisfy the publishing workflow guarantee.
